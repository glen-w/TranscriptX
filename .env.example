# Data root (optional; set in Docker to /data so app writes to the mounted volume)
# TRANSCRIPTX_DATA_DIR=/data

# Config root (optional; in Docker the image sets TRANSCRIPTX_CONFIG_DIR=/data/.transcriptx so config persists on the volume)
# TRANSCRIPTX_CONFIG_DIR=/data/.transcriptx

# Output root (optional)
TRANSCRIPTX_OUTPUT_DIR=./data/outputs

# Downloads: 1 = disabled (default), 0 = allow model/data downloads
TRANSCRIPTX_DISABLE_DOWNLOADS=1
# CLI: 0 = no emojis (e.g. CI), 1 = use emojis
TRANSCRIPTX_USE_EMOJIS=1

# Hugging Face token (optional; required for some diarization/model access)
# Set one or both: TRANSCRIPTX_HUGGINGFACE_TOKEN takes precedence.
# Two options so you can reuse HF_TOKEN if you already have it, or use the
# app-specific name (and a different token for TranscriptX if needed). See README.
TRANSCRIPTX_HUGGINGFACE_TOKEN=
HF_TOKEN=

# Logging
TRANSCRIPTX_LOG_LEVEL=INFO

# Database (optional)
TRANSCRIPTX_DB_ENABLED=1
