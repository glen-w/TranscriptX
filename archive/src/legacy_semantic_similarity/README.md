# Archived legacy semantic similarity modules

Archived in **0.9.3** as part of the public schema epoch.

Former live packages:

- `src/transcriptx/core/analysis/semantic_similarity/` (legacy)
- public ids `semantic_similarity` and `semantic_similarity_advanced` (both `legacy: True`)

The product path is now the former `semantic_similarity_v2` package, renamed to
`src/transcriptx/core/analysis/semantic_similarity/` with public module id
`semantic_similarity` (unversioned).

Do not re-enable these archived modules in the public registry.
