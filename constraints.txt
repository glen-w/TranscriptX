# Security baseline: use a setuptools version with published CVE/PYSEC fixes.
# If an environment must build very old llvmlite from source, prefer wheels or
# isolate that workflow with a dedicated constraints file.
setuptools>=78.1.1
