# RETIRED — do not install from this file.
#
# This used to be a partial freeze and drifted from live pins (e.g. pydantic,
# torch). Authoritative install sources for v1:
#   - pyproject.toml          (core + extras)
#   - requirements.txt        (Docker / transcriptx.sh full stack)
#   - constraints.txt         (security floors for Docker pip)
#   - uv.lock                 (resolved mirror of pyproject for uv users)
#
# See docs/dev/dependency_audit.md and docs/dev/v1_launch_readiness_2026-09-20.md.
