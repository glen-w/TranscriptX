# TranscriptX website

Modest public landing (plain HTML/CSS, minimal JS for mobile nav).

- Open `index.html` locally, or deploy via GitHub Pages (`.github/workflows/pages.yml`).
- Buy Me a Coffee URL is a placeholder until supplied (roadmap §20).
- Docs CTA points at in-repo docs until Read the Docs go-live (see `docs/dev/rtd_go_live_checklist.md`).
