# Read the Docs / Sphinx install pin list (mirrors pyproject [docs] extras).
# Prefer: pip install -e ".[docs]" from the repo root when developing locally.
sphinx>=7.0.0
myst-parser>=2.0.0
furo>=2023.9.10
sphinxcontrib-mermaid>=0.9.2
sphinx-autodoc-typehints>=1.24.0
