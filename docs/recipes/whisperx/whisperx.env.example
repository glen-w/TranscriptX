# WhisperX Docker Compose Environment Configuration
# Copy to whisperx.env and set your values.
# Never commit whisperx.env (it is gitignored).

# WhisperX Model Configuration
WHISPERX_MODEL=large-v2
WHISPERX_LANGUAGE=en
WHISPERX_COMPUTE_TYPE=float16
WHISPERX_DEVICE=cpu
WHISPERX_DIARIZE=true

# Hugging Face Token (required for diarization/model access)
HF_TOKEN=hf_your_token_here

# WhisperX Service Configuration (when using compose)
WHISPERX_SERVICE_URL=http://whisperx:8000
WHISPERX_CONTAINER_NAME=transcriptx-whisperx

# Volume Mounts
WHISPERX_INPUT_DIR=/data/input
WHISPERX_OUTPUT_DIR=/data/output
WHISPERX_TRANSCRIPTX_OUTPUT_DIR=/data/transcriptx_output

# Local Paths (for docker exec commands)
LOCAL_DATA_DIR=./data
LOCAL_OUTPUTS_DIR=./outputs
LOCAL_TRANSCRIPTX_OUTPUT_DIR=./transcriptx_output

# Note: batch_size, min_speakers, max_speakers are WhisperX CLI flags only;
# pass them via docker exec when invoking WhisperX (e.g. --batch_size 16).

# ---------------------------------------------------------------------------
# Host-side transcription (whispermlx on macOS; used from terminal / scripts)
# ---------------------------------------------------------------------------

# Path to whispermlx binary (optional if on PATH — check with: which whispermlx)
WHISPERMLX=

WHISPERMLX_MODEL=large-v3
WHISPERMLX_LANGUAGE=en
WHISPERMLX_DIARIZE=true
WHISPERMLX_TIMEOUT_SECONDS=0

# Default provider for programmatic transcription APIs (not Streamlit forms)
TRANSCRIPTX_TRANSCRIPTION_PROVIDER=whispermlx

# MP3 conversion defaults when staging audio for whispermlx (CLI / scripts)
TRANSCRIPTION_MP3_CODEC=libmp3lame
TRANSCRIPTION_MP3_BITRATE=128k
TRANSCRIPTION_MP3_CHANNELS=2
TRANSCRIPTION_MP3_SAMPLE_RATE=0
TRANSCRIPTION_FORCE_REENCODE=false
# Maps to "Keep intermediate staged MP3 files" on the request (not conversion options)
TRANSCRIPTION_KEEP_INTERMEDIATES=false
