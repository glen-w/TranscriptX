Type: CONTRACT
Authority: self

# Politeness group charts: pooled single view

## Inputs

- **`politeness_pooled`**: `total_marker_hits`, `by_category`; descriptive `mean_hits_per_100_tokens` and `mean_soft_request_ratio`.

## Chart

- `group.politeness.pooled.by_category.global` — bar of pooled category counts.

## Session bars

Curated session metrics via `PolitenessGroupChartGenerator`.

## Not

- Interaction equity / floor power (remains in `interactions`).
- Exact token-weighted rate pools.
