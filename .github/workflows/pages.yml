name: Deploy website + Sphinx docs to GitHub Pages

on:
  push:
    branches: [main]
    paths:
      - "website/**"
      - "docs/**"
      - "src/transcriptx/core/pipeline/module_specs.py"
      - "scripts/release/build_docs.sh"
      - "scripts/release/assemble_pages_site.sh"
      - "scripts/release/regen_module_docs.py"
      - "pyproject.toml"
      - "Makefile"
      - ".github/workflows/pages.yml"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
      - name: Install docs extras
        run: |
          python -m pip install -U pip
          pip install -e ".[docs]"
      - name: Refresh generated docs from registry
        run: make docs-gen
      - name: Assemble marketing site + Sphinx guide
        run: bash scripts/release/assemble_pages_site.sh
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: _site
      - id: deployment
        uses: actions/deploy-pages@v4
